services:
  daq-dev:
    build: .
    container_name: daq-firmware-dev
    volumes:
      - .:/workspace
      - ~/.gitconfig:/root/.gitconfig
      - /dev:/dev
    privileged: true
    working_dir: /workspace
    stdin_open: true
    tty: true
    platform: linux/amd64
